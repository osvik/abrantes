# Defer

This example uses scripts with the [defer](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/script#defer) attribute.

It's useful in pages with deferred [jQuery](https://jquery.com/) specially if you want to use jQuery in your experiment definitions.

* [Defer example](defer.html)

Using `defer` may impact the experiment performance.
