# Alpinejs

This examples show how to to AB testing using the [Alpine.js](https://alpinejs.dev/) framework.

1. Using [Alpine.$data](1.html) to modify existing Alpine blocks on a page.
2. Using [&lt;template&gt;](2.html) to define variants and place them with `x-teleport`
